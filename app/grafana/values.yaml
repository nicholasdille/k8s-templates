#@data/values
---
#@overlay/match missing_ok=True
timezone: Europe/Berlin
grafana:
  namespace: grafana
  db:
    matchLabels:
      pod: mysql-grafana
  server:
    matchLabels:
      pod: grafana
  database:
    name: grafana
    username: grafana
    password: test123
  url: https://grafana.example.com
  admin:
    password: test123
