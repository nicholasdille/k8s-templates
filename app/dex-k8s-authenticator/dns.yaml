#@ load("@ytt:data", "data")
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: k8s-authenticator
  namespace: #@ data.values.k8sauth.namespace
spec:
  endpoints:
  - dnsName: #@ "{}.{}".format(data.values.k8sauth.dns.name, data.values.domain)
    recordTTL: 180
    recordType: CNAME
    targets:
    - #@ "{}.{}".format(data.values.k8sauth.dns.lb, data.values.domain)
